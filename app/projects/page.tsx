import ProjectsShowcase from "../components/ProjectsShowcase";

const page = () => {
  return (
    <section className="min-h-reduced py-20 flex flex-col bg-gradient-to-t from-violet-dark-a40 via-transparent">
      <ProjectsShowcase />
    </section>
  );
};

export default page;
